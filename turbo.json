{
	"$schema": "https://turborepo.org/schema.json",
	"baseBranch": "origin/main",
	"pipeline": {
		"typescript": {
			"dependsOn": ["^typescript"],
			"outputs": ["dist/**"]
		},
		"build": {
			"dependsOn": ["^build", "typescript"],
			"outputs": ["dist/**"]
		},
		"build-github": {
			"dependsOn": ["^build-github", "typescript"],
			"outputs": ["dist/**"]
		},
		"esbuild": {
			"dependsOn": ["^esbuild"],
			"outputs": ["dist/**"]
		},
		"dev": {
			"cache": false
		}
	}
}
