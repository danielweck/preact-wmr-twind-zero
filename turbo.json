{
	"$schema": "https://turborepo.org/schema.json",
	"baseBranch": "origin/main",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["packages/**/dist/**"]
		},
		"typescript": {
			"dependsOn": ["^typescript"],
			"outputs": ["packages/**/dist/**"]
		},
		"build-github": {
			"dependsOn": ["^build", "^build-github"],
			"outputs": ["packages/**/dist/**"]
		},
		"dev": {
			"cache": false
		}
	}
}
