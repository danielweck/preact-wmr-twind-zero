/** @type {import('@twind/core').TwindUserConfig<import('@twind/core').BaseTheme>} */
export const twindConfig = {
	preflight: false, // just so we can manually check the generated stylesheet more easily
	hash: false,
};
export default twindConfig;
